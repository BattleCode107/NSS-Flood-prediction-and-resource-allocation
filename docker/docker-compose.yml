version: '3.8'

services:
  dashboard:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    container_name: nirvana-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ../data:/app/data
      - ../models:/app/models
    environment:
      - STREAMLIT_ENV=production
